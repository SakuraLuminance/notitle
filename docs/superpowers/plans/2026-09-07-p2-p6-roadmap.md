# AnaPlug UI/DSP 重构路线图（P2–P6 总纲）

日期：2026-09-07 · 由 P1 批准流程延续（**P1 已完成：Run #106 全绿（541 用例 + strictness 2），spec: `docs/superpowers/specs/2026-09-07-p1-live-spectrum-design.md`，plan: `docs/superpowers/plans/2026-09-07-p1-live-spectrum.md`，HEAD 993aadf，成品已装 VST3**）

> **执行规则（沿用本项目铁律）**
> 1. 本机无编译工具链——一切验证走 GitHub Actions（~20 min/轮）；push 用 `git push https://x-access-token:<PAT>@github.com/SakuraLuminance/notitle.git main`，push 后 `git fetch origin main` 刷新本地跟踪 ref；PAT 由用户会话内提供，不落盘。
> 2. 每个子项目独立走 brainstorming（设计确认）→ spec → plan → 实现 → 单轮 CI 取证；子项目内部可用多轮。
> 3. 基线：Run #102 起 **539 用例 0 失败 + pluginval strictness 2 绿**；不得回归。
> 4. CI 取证工作流照 HANDOFF_V2 §6（认证 API 拉 jobs/logs + test-forensics artifact）。
> 5. 编辑器构造路径 bug 家族教训在 HANDOFF_V2 §0.2 末三条——P2-P6 大量新增 UI 组件，**新面板的 ctor 内禁止调用会同步触发自身 resized() 的 setSize（若必须，则 resized() 内所有 unique_ptr 成员判空）**；所有 setSelectedId 的 onChange lambda 必须容忍 processor 未 prepare 的状态。
> 6. 用户反馈原文要点（2026-09-07）：频谱不显示（P1 修）；效果器栏不知所云/不能改参数（P2）；UI 臃肿详略不当、导入难找（P3）；包络缺失（P4）；想要粒子/进化等前沿功能（P5）；想要 Harmor 式基于采样的减法（P6）。参考系：Harmor、Phase Plant、Serum 取百家之长。

---

## P2 — 效果器栏 Phase-Plant 化（**第一批 8 类型已完成**：Run #112 全绿 551 用例，HEAD cf6c407；剩余 = 第二批 6 类型参数表 EQ(9 参)/AutoTune/Flanger/Phaser/RingMod/StereoWidener + 枚举参数菜单控件 + rack 内 MIDI Learn；架构（EffectBase 参数面/Registry/Panel/展开）已全部就位，加类型即加表）

**问题**：EffectRackComponent 其实已是 Phase Plant 式骨架（`EffectSlotWidget`：加/删/上下移/bypass/mix/lo/hi/mod + `+ Add Effect` 菜单 + UndoableAction），但**每个效果器模块自己的参数完全没有 UI**——每槽只有 3-4 个通用滑条，Rate/Size/Tone/Depth 等"真正的"参数无从下手，所以"不知所云"。

**目标形态**：点击槽位展开"参数编辑行"（Phase Plant 式），按效果类型渲染专属 knob 集；折叠恢复 32px 紧凑行。

**侦察清单（开工前必查）**：
- `src/gui/EffectRackComponent.cpp`：EffectSlotWidget 现在怎么把 mix/lo/hi/mod 写回 EffectsChain（数据通道与节流方式）；`getAvailableEffectTypes()` / `createEffectByName()` 的类型名清单；`rebuildSlots()` 触发时机。
- `src/dsp/EffectsChain.{h,cpp}`：slot 数据结构（EffectBase 接口：有哪些通用参数访问器？getState/setState？）；`PresetFactory`/`ProcessorStore` 里模块类型注册表。
- 目标模块各自的参数面：优先 8 个常用（Drive、Chorus、Delay、Reverb、Distortion、EQ、Compressor、Limiter）→ 第二批（Phaser/Flanger/RingMod/Bitcrusher/Saturation/StereoWidener/DeEsser/Dynamics…）。每个模块列出：参数 id/中文名/范围/默认值/读取与写入 API；**没有访问器的模块需要补**（保持向后兼容：PresetManager round-trip 测试必须仍绿）。

**设计骨架**：
- `EffectParamPanel`（新组件）：接收 `EffectBase&` + 模块类型 id，内部按注册表渲染 knob/滑条行；注册表 = `std::map<juce::String, std::vector<ParamSpec>>`（ParamSpec：id、label、min、max、default、skew、getter、setter——用 std::function 绑定到具体模块实例）。
- EffectSlotWidget 增加展开按钮（▾/▸），展开时在自身下方插入 60-90px 的 EffectParamPanel（在 rack 的 contentPanel 里 reserve 高度；viewport 自动滚）。
- 节流：拖 knob → 立即调 setter（参考现有 mixSlider 的通道；若现有通道有 timer 聚合则沿用，避免每 sample 更新）。
- MIDI Learn：新参数顺手注册（沿用 `setupMidiLearnForSlider` 模式需要编辑器指计——rack 在 editor 内部，可给 rack 传一个注册回调）。
- **护栏**：541 用例 + pluginval strictness 2；effect preset round-trip 系列测试是回归红线。

**验收**：加任意常用效果 → 展开能看专属参数名（不缩写黑话）→ 拖动即时生效 → 折叠/展开/重排不崩；preset 保存/加载带这些参数。

**规模估计**：2 轮 CI（第一批 8 模块 + 框架；第二批余量模块）。

---

## P3 — 信息架构重组（Serum/Harmor 式分页）

**问题**：1100×780 里 7 个区域同屏（Timbre A/中央/ effects/mod/底部六合一/状态栏），详略失衡；LOAD（导入采样）是右下角小按钮。

**目标形态**（建议稿，需用户确认后写 spec）：
- 标题栏：`[IMPORT SAMPLE]`（大按钮，原 LOAD 语义）+ PRESET + LIVE 频谱常驻中央。
- 主区改 **tab 分页**（左侧竖排或顶部横排，Serum 式）：`TIMBRE | FILTER | MOD | SEQ | FX | MASTER`：
  - TIMBRE：Timbre A/B + blend（P5 接线后全部生效）
  - FILTER：FilterPanel（现有）
  - MOD：ModulationAssignPanel + P4 的包络编辑器 + LFO
  - SEQ：SequencerPanel（现有）
  - FX：EffectRackComponent（现有，P2 增强）
  - MASTER：MasterSection + metering + Unison/Voice/Arp（这三个要不要独立 PLAY 页，问用户）
- 中央频谱（LIVE/PARTIALS…视图组）**脱离 tab、常驻**（Serum 的顶部 oscilloscope 模式）。
- 底部状态栏保留（status/dna/credits/randomize/metering）。

**侦察清单**：tab 容器实现选型（juce::TabbedComponent vs 手写按钮组——主题一致性）；各 panel 的最小尺寸约束（TimbrePanel 17% 宽的像素级布局假设在 tab 页内如何重排——Phase 3 的 panels 布局是按"占据原区域"写的，进 tab 后要按新区域重写 resized 的百分比，**但控件几何只需面板内部协调，无跨面板耦合**）；modViewport/modPanel 的 setSize(300, calcContentHeight) 约定。

**验收**：所有现有控件在分页后仍可达且不丢状态；541+ 测试与 pluginval 绿；用户主观"不臃肿"。

**规模估计**：1-2 轮 CI。

---

## P4 — 画布式包络 / LFO 编辑器

**问题**：DSP 有完整 `MultiPointEnvelope`（断点/曲线/loop 模式，tests 已覆盖）与 `LFOSystem`、`ModulationBus`、`runEnvPool`，但 UI 只有 ModulationAssignPanel 顶部 4 个 ADSR 滑条；没有画布。

**目标形态**：
- `EnvelopeCanvas`：断点拖拽（值/时间）、每段曲线形状（MultiPointEnvelope 的段曲线契约：曲线属**段末**断点，见 HANDOFF §1 契约）、loop 区间（Forward/PingPong，注意"loop 点未配置 = play once"语义）、release 行为。
- ENV 槽位：先接 Volume ADSR（现有 4 滑条迁入画布）+ ENV1/ENV2（runEnvPool 已有池语义）。
- 指派：复用 ModulationAssignPanel 的 section/rows 数据（目标参数下拉 + depth），ENV 作为 source 加入 source combo。
- LFO：波形可视化 + rate/depth（LFOSystem 自定义波形编辑列为可选项——侦察后定）。

**侦察清单**：MultiPointEnvelope 公开 API（setPoints/setCurve/setLoop/isActive/process）；runEnvPool 在 processor 侧的暴露方式；ModulationAssignPanel 的 row→slotIndex 映射与 ModulationMatrix 关系；LFOSystem 波形种类与自定义程度。

**验收**：画 ENV1 → 指派 filter cutoff → 播放可闻扫频；headless paint + envelope 单元测试。

**规模估计**：2 轮 CI（画布+Volume；ENV1/2+指派）。

---

## P5 — 前沿功能接线（粒子 / 进化 / DualTimbre）

**现状（已代码实证）**：`SpectralParticleSystem`、`DualTimbre`、`GenerativeTimbreDesigner`、`SpectralFreezeEngine`、`QuantumSpectralProcessor`、`HPSSEngine` **全部已编译进插件但 processor/engine 零引用**；`SpectralDNA/DNAEvolver` 已接线（randomizeAllParameters + EvolutionPanel callout + DNA EVOLVE 按钮）；`SpectralMorpher::morphLinear` 与 `WavetableEngine` 部分接线（morphAmount、wavetableEnabled_）。

**目标形态**：
1. **粒子系统**：作为视图模式（频谱区新增 "PARTICLES"）+ 触发链路（engine 处理 partials 时喂给粒子引擎，UI 画粒子）——先读 `SpectralParticleSystem.h` API 再定（侦察项①）。
2. **DNA 进化融入主流程**：EvolutionPanel 从 callout 升为主 MOD/TT 页签的常驻面板；"EVOLVE TIMBRE" 一键用 fittest partial 做当前 timbre（侦察项②：dnaEvolver→engine 的桥）。
3. **DualTimbre A/B**：TimbrePanel A/B 现在只有 sub 生效（bright/blur/hpf 死）——DualTimbre.h API（侦察项③）很可能就是为 A/B 设计的；接上后 BLEND 滑条（现在死）才有意义。
4. **SpectralFreeze / GenerativeTimbreDesigner / HPSS / QuantumSpectral**：逐个评估价值与风险，能接就接（freeze 按钮 = 频谱冻结，Serum 无此能力，卖点）。

**验收**：每个接线功能：可从 UI 触发 + 有单元测试 + pluginval 绿。**不接线就不出现在 UI**（避免再造死控件）。

**规模估计**：2-4 轮 CI（按接线数量）。

---

## P6 — Harmor 式采样减法补全

**问题**：引擎本就是"采样→STFT→partials→操作"的 Harmor 架构，但用户可用的减法工具残缺：Timbre 面板 bright/blur/hpf 死、BLEND 死；`PartialEditorCanvas`（partial 拖拽编辑）**整个文件存在但从未接入 UI**（test_partial_editor_canvas.cpp 也被注释禁用）。

**目标形态**：
- 死控件激活（每个先侦察引擎对应 API——侦察项④：`AnaPlugEngine`/`FrequencyShaper`/`BlurEffect` 的 per-side 接口）：
  - bright→频谱倾斜（FrequencyShaper？）、blur→部分模糊量（引擎内已有 blur 处理？与 BLUR 按钮 setBlurEnabled 的关系要理清）、HPF→分析/合成侧高通、blend→A/B 混合权重（依赖 P5 DualTimbre）。
- Partial 编辑：SpectrumEditorCanvas/PartialEditorCanvas 合并或择一，接入 EDITOR 视图页；拖拽改 partial 幅度/频率 → 引擎下一帧生效。
- 颗粒/时间：GranularSynthesizer 已修好采样精确调度（Run #96 前）——暴露成 UI（grain 密度/长度）列为可选。

**验收**：加载采样 → EDITOR 页拖 partial 生效；timbre 控件有可闻效果；全部 preset round-trip 绿。

**规模估计**：2-3 轮 CI。

---

## 建议执行顺序与依赖

```
P2(效果器参数) ──┐
P3(信息架构) ────┼─→ 依赖关系：P3 改变面板归宿，P4/P5 的新面板应直接放进 P3 后的布局；
P4(包络) ────────┤   因此推荐：P2 → P3 → P4 → P5 → P6
P5(前沿接线) ────┘   （P5 的 DualTimbre 是 P6 blend 的前置）
P6(采样减法) ─────   （P6 的 blend 依赖 P5-3）
```

用户可随时改顺序（P2 与 P3 互换的代价：P4/P5 面板要适配方一次）。

## 给下一个会话的启动检查单

1. `git fetch origin main && git log --oneline -5` 确认基线（**P1 完成 = 993aadf**，Run #106 全绿 541 用例 + strictness 2；成品 artifacts/106 已装 VST3 + REAPER 缓存已清）。
2. ~~查 Run #103~~（已定案：Run #103-105 三红 = include 路径 + clap-juce-extensions 陷阱，993aadf 修复，Run #106 绿）。
3. P2 开工：先跑侦察清单（EffectRackComponent.cpp + EffectsChain.h 全读），再 brainstorming→spec→plan→实现。**新教训**：任何被测试目标编译的文件严禁 include PluginProcessor.h（clap 陷阱，见 HANDOFF §0.2）。
4. 若 PAT 失效（401）：向用户要新 token。
5. pluginval strictness 2→3 的升级可择机与 P2 首轮合并推。
